#include <stdio.h>  // Standart Giriþ/Çýkýþ kütüphanesi (printf için gereklidir)
#include <stdlib.h> // Standart Kütüphane (bazý sistem fonksiyonlarý için, bu kodda opsiyoneldir)

int main()
{
    /* printf fonksiyonu:
       - Ekrana formatlý çýktý vermek için kullanýlýr.
       - Parantez içindeki çift týrnak (" ") arasýna yazýlan metni konsola basar.
       - \n ifadesi "new line" (yeni satýr) anlamýna gelir, imleci bir alt satýra indirir.
    */
    printf("Hello, this is my first project!\n");

    return 0; // Programýn baþarýyla tamamlandýðýný iþletim sistemine bildirir.
}
